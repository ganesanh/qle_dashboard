# Runtime Storage

This directory is for runtime-generated files only.

Expected subdirectories are created automatically by the server:

- `bundles/`
- `formatted/`
- `logs/`
- `tmp/`
- `uploads/`

Do not commit generated runtime artifacts from this directory.
